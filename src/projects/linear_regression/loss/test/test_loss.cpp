#include "loss/loss.h"

#include "math/mat_n.h"
#include "gtest/gtest.h"

namespace kioku
{

TEST(LossTest, L1) { ASSERT_TRUE(true); }

TEST(LossTest, L2) { ASSERT_TRUE(true); }

}  // namespace kioku
